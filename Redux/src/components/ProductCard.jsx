import React from 'react';

const ProductCard = () => {
    return (
        <div>
            <h2>Ürün Kartı</h2>
            <p>Bu, bir ürün kartı örneğidir.</p>
        </div>
    );
};

export default ProductCard;

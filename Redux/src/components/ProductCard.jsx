import React from 'react';

const ProductCard = ({ dt }) => {
    return (
        <div className="w-[200px] h-[200px] relative ">
            <h2>Ürün Kartı</h2>
            <p>Bu, bir ürün kartı örneğidir.</p>
        </div>
    );
};

export default ProductCard;
